<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- Assuming 'notes' is an array of notes fetched from the database -->
<ul>
    <% for (var i = 0; i < notes.length; i++) { %>
      <li><%= notes[i].Note %></li>
    <% } %>
    
  </ul>
</body>
</html>